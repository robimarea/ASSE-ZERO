# Showreel Assets

Metti qui i media locali usati dalla sezione `Showreel`.

Convenzione consigliata:

- `*.mp4` per i video
- `poster-*.png` per i poster dei video
- `still-*.jpg` / `still-*.png` / `still-*.svg` per le immagini fisse

La sorgente dati e` in `src/data/showreelAssets.ts`.
